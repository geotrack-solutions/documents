# Docsify API Docs Starter

## Local preview
```bash
npm i -g docsify-cli
docsify serve docs
```
Then open http://localhost:3000

## Deploy
- **GitHub Pages:** enable Pages on the repository and select **/docs** folder on `main`.
- **Netlify/Cloudflare Pages/Azure Static Web Apps:** set the publish directory to `docs`.
