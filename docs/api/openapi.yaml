openapi: 3.0.3
info:
  title: Sample API
  version: "1.0.0"
  description: |
    Minimal OpenAPI spec to show Swagger-UI embedded in Docsify.
servers:
  - url: https://api.example.com/v1
paths:
  /ping:
    get:
      summary: Health check
      operationId: ping
      responses:
        '200':
          description: API is reachable
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
